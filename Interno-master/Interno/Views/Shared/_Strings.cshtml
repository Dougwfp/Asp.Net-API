<script>
    var urlGridTransport = '@Url.Action("index", "transports")';
    var urlInfoPaginationTransport = '@Url.Action("getInfoPagination", "transports")';
    var urlInfoTransport = '@Url.Action("Index", "transports_c")';
    var urlEditTransport = '@Url.Action("Edit", "transports")';
    var urlDeleteTransport = '@Url.Action("Delete", "transports")';

    var urlGridCliente = '@Url.Action("index", "clientes")';
    var urlInfoPaginationCliente = '@Url.Action("getInfoPagination", "clientes")';
    var urlInfoCliente = '@Url.Action("Index", "clientes_c")';
    var urlEditCliente = '@Url.Action("Edit", "clientes")';
    var urlDeleteCliente = '@Url.Action("Delete", "clientes")';

</script>